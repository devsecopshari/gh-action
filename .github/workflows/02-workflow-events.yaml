name: 02-workflow-events

on: push

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: show the trigger
              run: echo "i am the triggera(n) ${{ github.event_name}} events."

